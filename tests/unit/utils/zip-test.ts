import zip from 'minecraft-datapack-creator/utils/zip';
import { module, test } from 'qunit';

module('Unit | Utility | zip', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = zip();
    assert.ok(result);
  });
});
