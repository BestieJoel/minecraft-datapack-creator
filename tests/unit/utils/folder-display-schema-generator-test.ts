import folderDisplaySchemaGenerator from 'minecraft-datapack-creator/utils/folder-display-schema-generator';
import { module, test } from 'qunit';

module('Unit | Utility | folder-display-schema-generator', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = folderDisplaySchemaGenerator();
    assert.ok(result);
  });
});
